I see that it is running an OpenMP application that has a function called doComputation. 
I also noticed an odd pattern for when each processer runs doComputation and TauTest, but it was explained to me that TauTest reports anything that would run as main on a processor as TauTest.
